s and applications, the data should be validated. In particular, the following items should be checked:

The onboarding history does not show any failures

The users have been onboarded into the correct group

The users are owners of their corresponding computer

The correct applications list exists against the user’s corp id

Validation Stage Process

The following activities are performed as part of the validation stage process:

Amend request type from VDI (if applicable for laptop users)

Re