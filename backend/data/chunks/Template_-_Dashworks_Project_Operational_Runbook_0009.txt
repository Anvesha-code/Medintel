the business logic for the Dashworks application. It serves static and dynamic web pages as well as managing database access, security, and transaction services for Dashworks. The Dashworks data scrape processes are scheduled on the database server and use vendor code to login to, and incrementally scrape the required data repositories. The main objective for the Dashworks data scrape service is to provide a standard mechanism for storing and accessing critical project information. The HR database sends personnel data such as names, locations and departments to Dashworks.